# Example env for mrazota-site
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=
PAYSERA_CHECKOUT_URL=
PAYSERA_SIGN_PASSWORD=
PAYSERA_SIGN_ALGO=hmac-sha256
BANK_DETAILS_KEY=
DATABASE_URL=postgresql://user:pass@localhost:5432/mrazota
JWT_SECRET=change_me
NODE_ENV=production
# Local environment example for the Next.js site
# Copy to `.env.local` and replace placeholder values. Do NOT commit `.env.local` with real secrets.

# App host (used to build absolute URLs for callbacks/returns)
HOST_URL=http://localhost:3000

# Database (example for Prisma)
DATABASE_URL="file:./dev.db"

# JWT secret for auth
JWT_SECRET=your_jwt_secret_here

# Paysera integration (fill with real values from your Paysera account)
# - PAYSERA_PROJECT_ID: numeric project/shop id provided by Paysera
# - PAYSERA_SIGN_PASSWORD: secret pass used to sign/verify payloads
PAYSERA_PROJECT_ID=your_paysera_project_id
PAYSERA_SIGN_PASSWORD=your_paysera_sign_password
# Paysera endpoints (built from HOST_URL in production/automations)
PAYSERA_RETURN_URL=${HOST_URL}/paysera/return
PAYSERA_CANCEL_URL=${HOST_URL}/paysera/cancel
PAYSERA_CALLBACK_URL=${HOST_URL}/api/paysera/webhook

# Optional: which signing algo to use (hmac-sha256/hmac-sha1/md5)
PAYSERA_SIGN_ALGO=hmac-sha256

# Optional encryption key for bank details (base64 / 32 bytes)
BANK_DETAILS_KEY=

# Settlement account placeholders (do NOT commit real values)
SETTLEMENT_ACCOUNT_IBAN=
SETTLEMENT_ACCOUNT_BIC=
SETTLEMENT_ACCOUNT_NAME=
SETTLEMENT_ACCOUNT_BANK_NAME=
