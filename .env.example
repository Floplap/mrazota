## Consolidated environment variables required to run the project
# Copy this to `.env` (or use Hostinger's environment variable settings) and fill values.

# Supabase
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# Database (optional alternative to service role)
DATABASE_URL=postgres://user:pass@host:5432/dbname

# Paysera (payment provider) - if you use payments
PAYSERA_PROJECT_ID=your_paysera_project_id
PAYSERA_SIGN_PASSWORD=your_paysera_sign_password
PAYSERA_SECRET=your_paysera_secret
PAYSERA_RETURN_URL=https://your-site.example/paysera/return

# App settings
HOST_URL=https://your-site.example
PORT=3000
JWT_SECRET=replace_with_a_long_random_string

# Optional
NODE_ENV=production
# Repository-level environment example
# Copy to `.env` (or use CI secrets) and fill with real values. Do NOT commit real secrets.

# Public host used by automated scripts to derive callback URLs
HOST_URL=http://localhost:3000

# Paysera (global defaults for scripts that touch multiple services)
PAYSERA_PROJECT_ID=your_paysera_project_id
PAYSERA_SIGN_PASSWORD=your_paysera_sign_password
PAYSERA_SIGN_ALGO=hmac-sha256
PAYSERA_RETURN_URL=${HOST_URL}/paysera/return
PAYSERA_CANCEL_URL=${HOST_URL}/paysera/cancel
PAYSERA_CALLBACK_URL=${HOST_URL}/api/paysera/webhook

# Supabase example (if used by backend)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Optional: global JWT secret
JWT_SECRET=some-jwt-secret
