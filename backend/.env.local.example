# Copy this file to `backend/.env.local` and fill in real values. Do NOT commit the real `.env.local`.
# Example entries (replace values):
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
JWT_SECRET=some-random-secret
# Paysera integration
PAYSERA_PROJECT_ID=your-paysera-project-id
PAYSERA_SIGN_PASSWORD=your-paysera-sign-password
# Callback/return URLs (use your public host URL)
PAYSERA_RETURN_URL=https://example.com/paysera/return
PAYSERA_CANCEL_URL=https://example.com/paysera/cancel
PAYSERA_CALLBACK_URL=https://example.com/api/paysera/webhook
PAYSERA_SIGN_ALGO=hmac-sha256
PORT=5000
